name: worker

on:
  push:

jobs:
  blank:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        extra: ["compress", "coil", "illustration", "frequent", "elementary"]
        passage: ["private", "ache", "extensive", "summary"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar Ez
